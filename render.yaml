services:
  - type: web
    name: media-telegram-bot
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    plan: free
    envVars:
      # توکن را در داشبورد Render وارد کنید (Environment → Add Environment Variable)
      - key: BOT_TOKEN
        sync: false
      # RENDER_EXTERNAL_URL به‌طور خودکار تنظیم می‌شود و برای webhook استفاده می‌گردد.
